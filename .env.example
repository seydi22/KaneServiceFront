# ===========================================
# Frontend - Variables à définir sur Vercel
# (Settings > Environment Variables du projet)
# ===========================================

# Obligatoire quand le backend est sur un autre domaine.
# IMPORTANT : l'URL doit se terminer par /api (les routes sont sous /api/auth, /api/operations, etc.)
# Exemple : https://kane-servicebackend.vercel.app/api
VITE_API_URL=https://kane-servicebackend.vercel.app/api

# Si frontend et backend sont sur le même domaine, ne pas définir VITE_API_URL.
